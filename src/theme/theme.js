import { makeStyles } from '@material-ui/core/styles';

export const useStyles  = makeStyles({
    nomeForm: {
      width: "100%",
    },
    comboBox: {
      marginTop: 20,
      marginBottom: 20
    },
});